{
  "name": "redux-saga-auth",
  "version": "0.0.1",
  "description": "This library is a proposal of redux-saga auth flow",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "babel src --out-dir . --presets env"
  },
  "keywords": [
    "redux-saga",
    "auth",
    "flow"
  ],
  "author": "Alexander Veligurov",
  "license": "ISC",
  "dependencies": {
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1",
    "fbjs": "^0.8.15",
    "redux-actions": "^2.2.1",
    "redux-saga": "^0.15.6"
  }
}
